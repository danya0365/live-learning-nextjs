/**
 * (fullscreen) Layout
 * Clean, distraction-free — no Header, no Footer, no orbs
 * Used for: auth, booking flow, live room
 */

import { type ReactNode } from 'react';

export default function FullscreenGroupLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
